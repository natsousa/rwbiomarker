% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomarker.R
\name{add_biomarker}
\alias{add_biomarker}
\title{Attach a simulated tumor biomarker to a cohort}
\usage{
add_biomarker(data, prevalence = 0.35, seed = 7)
}
\arguments{
\item{data}{A data frame produced by \code{\link[=load_colon]{load_colon()}} (must contain a
\code{differ} column).}

\item{prevalence}{Numeric in (0, 1). Target proportion of biomarker
positive patients.}

\item{seed}{Integer. RNG seed for reproducibility.}
}
\value{
The input tibble with a \code{biomarker} factor column appended,
levels \code{c("Negative", "Positive")}.
}
\description{
Adds a binary biomarker (e.g., MSI-high vs MSS) to the input cohort.
The latent score is correlated with tumor differentiation grade so
the biomarker has biological plausibility, then thresholded to hit a
user-specified prevalence.
}
\examples{
d <- add_biomarker(load_colon(), prevalence = 0.30, seed = 1)
prop.table(table(d$biomarker))
}
